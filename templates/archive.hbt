{{> header}}
    <div class="content">
      <h2>archive</h2>
      <ul>
        {{#each collections.posts}}
            <li>
                <a class="title" href="{{this.blogPath}}">{{this.title}}</a>
                <article>{{{this.excerpt}}}</article>
            </li>
        {{/each}}
      </ul>
    </div>
{{> footer}}